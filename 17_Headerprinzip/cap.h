void flipchar(char str[]);
void delChar(char str[], int index);
void strCopy(char tgt[], char src[],  int maxLen); // "normal" nicht zu verwenden
                                      // lieber strncopy (mit )
